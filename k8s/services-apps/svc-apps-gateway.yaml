apiVersion: v1
kind: Service
metadata:
  name: svc-gateway
  namespace: tech-challenge
  labels:
    app: gateway
    component: api-gateway
spec:
  selector:
    app: gateway
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
