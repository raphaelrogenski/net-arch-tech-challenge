apiVersion: v1
kind: Service
metadata:
  name: svc-sqlserver
  namespace: tech-challenge
  labels:
    app: sqlserver
    component: tools
spec:
  selector:
    app: sqlserver
  ports:
    - port: 1433
      targetPort: 1433
  type: ClusterIP
