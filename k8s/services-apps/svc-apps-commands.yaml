apiVersion: v1
kind: Service
metadata:
  name: svc-commands
  namespace: tech-challenge
  labels:
    app: commands
    component: app
  annotations:
    description: "API to receive write operations"
spec:
  selector:
    app: commands
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
