apiVersion: v1
kind: Service
metadata:
  name: svc-prometheus
  namespace: tech-challenge
  labels:
    app: prometheus
    component: tools
spec:
  selector:
    app: prometheus
  ports:
    - port: 9090
      targetPort: 9090
  type: ClusterIP
