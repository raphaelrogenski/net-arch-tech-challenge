apiVersion: v1
kind: Service
metadata:
  name: svc-dlqmonitor
  namespace: tech-challenge
  labels:
    app: dlqmonitor
    component: function
spec:
  selector:
    app: dlqmonitor
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
